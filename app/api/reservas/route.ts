import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'GET /api/reservas — pendiente implementación' })
}

export async function POST() {
  return NextResponse.json({ message: 'POST /api/reservas — pendiente implementación' })
}
